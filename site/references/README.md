# Staged references

The Pages workflow copies `../../references/*` into this directory at deploy time
so the static site can fetch markdown/CSV same-origin. Do not hand-edit copies here;
edit the repo-root `references/` tree.
